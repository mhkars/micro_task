server:
  port: 8092

spring:
  security:
    user:
      name: halit
      password: 123
  datasource:
    username: 'postgres'
    password: 'root'
    driver-class-name: org.postgresql.Driver
    url: 'jdbc:postgresql://localhost:5432/VehicleDB'

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
